name: Release
on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to release'
        type: string
        required: true

jobs:
  on-success:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The Run Tests workflow passed'
